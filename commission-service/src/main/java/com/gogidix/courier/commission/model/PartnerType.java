package com.gogidix.courier.commission.model;

public enum PartnerType {
    COURIER,    // Individual courier partners
    VENDOR,     // Vendor partners (e.g., stores, restaurants)
    FRANCHISE,  // Franchise partners (operating under our brand)
    AGGREGATOR, // Aggregator partners (e.g., delivery networks)
    ENTERPRISE  // Enterprise partners (large corporate clients)
}
